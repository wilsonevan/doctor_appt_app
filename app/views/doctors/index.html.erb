<h1>Doctors Available</h1>
<hr />
<h2><%= @doctors.length <= 0 ? "No Doctors" : "" %></h2>
<ul>
  <% @doctors.each do |doctor| %>
    <li><%= link_to "Dr. #{doctor.name}", doctor %></li>
  <% end %>
</ul>
